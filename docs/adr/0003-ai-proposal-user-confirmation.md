# ADR-0003：AI 建议由软件执行且需人工确认

状态：已接受

## 决策

DeepSeek 只能返回受限的结构化计划变更建议。软件完成校验和差异预览，使用者
确认后由应用服务创建新计划版本。AI 不持有数据写权限。

## 原因

这使计划调整可解释、可审计和可回滚，并阻止模型绕过医嘱、红黄绿规则或误用
过期计划。Garmin 数据同样只能建议关联，不能自动完成任务。

## 结果

每条建议绑定基础版本；红灯建议不能自动应用；版本已变化时建议过期并重新分析。
