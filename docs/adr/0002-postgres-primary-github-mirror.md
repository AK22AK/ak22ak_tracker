# ADR-0002：PostgreSQL 为主、GitHub 为镜像

状态：已接受

## 决策

Neon PostgreSQL 是在线业务主数据库，私有 GitHub 数据仓库是通过 outbox 异步
维护的结构化镜像。

## 原因

计划、反馈、幂等写入、关联查询和同步游标适合数据库事务；GitHub 文件适合人工
检查、版本追踪和供其他 AI 读取，但不适合承载每次在线交互的查询与并发写入。

## 结果

数据库写入成功即完成用户操作。GitHub 短暂失败只显示镜像延迟并自动重试；恢复
时可以从数据库重新生成镜像。
