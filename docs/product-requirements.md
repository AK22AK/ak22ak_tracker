# 产品需求

## 文档职责

本文是 AK Tracker 的产品目标、用户旅程、第一版范围和成功标准的权威来源。系统
怎样实现见[系统架构总览](architecture/overview.md)，当前完成度和实施顺序见
[项目计划](project-plan.md)，界面规范见[产品与 UI/UX 设计系统](design/product-and-visual-system.md)。

## 定位

第一版只服务单个使用者，不按商业产品设计。它在手机上持续回答四个问题：

1. 今天应该做什么？
2. 今天实际做了什么？
3. 身体对训练的反应如何？
4. 明天的计划是否需要调整？

核心能力采用通用 tracker 模型，膝关节康复是第一个模块。以后可以增加护肤等
模块，但第一版不会为未知需求增加多用户、社交、付费或运营能力。

## 信息架构

受保护的应用区域固定包含四个顶层页面：

- `今日`：当天任务、实际执行、反馈和待处理事项。
- `日历`：未来计划与历史执行、反馈和异常上下文。
- `计划`：当前计划、下一训练、康复助手、待确认建议、回顾、评估、版本、档案和记忆。
- `设置`：集成、同步、通知、导出、账号和本机数据。

底部导航始终可操作并保留各页面状态。网络数据不得阻塞日期、Tab、折叠区和表单
等本地交互；详细规则见[客户端数据与导航](architecture/client-data-and-navigation.md)。

## 第一版用户流程

### 今日计划

- 打开手机 PWA 后直接看到当天有效计划版本及任务。
- 每项任务都是待办选框，只能由使用者确认完成或跳过。
- 显示当日反馈是否完成、数据同步状态和待确认的调整建议。

### 执行记录

- 可按计划勾选，也可记录计划外训练。
- 使用者已经在训记中记录的动作、重量、组次、次数和训练感受，由应用只读同步，
  不要求在 AK Tracker 再抄写一次；手工训练表单只作为同步不可用或记录缺失时的
  兜底。
- Garmin 是活动发生时间、时长、距离、配速、心率和基础睡眠的主要来源；训记是
  力量训练动作、组次、重量、次数和 RPE 等训练细节的主要来源。两者可以同时关联
  同一项任务，互相补充而不是互相覆盖。
- 系统根据计划、日期和训练内容提出关联建议；外部记录关联和任务完成状态都由
  使用者确认，任何同步结果都不能自动勾选任务。
- AK Tracker 保留来源、关联决定和必要的主观补充，并把确认后的实际执行提供给
  后续趋势和 AI 分析。

### 身体反馈

- 每天至少提醒一次结构化反馈；完成后弱化提醒，但仍允许追加反馈。
- 膝关节模块优先记录疼痛、肿胀、功能表现、训练后及 24 小时反应。
- 突发情况可以随时新增，不覆盖当天早先的记录。
- 可以使用系统语音输入或自然语言快速描述感受；AI 只能把描述整理成待确认草稿，
  结构化结果仍由使用者确认后保存。
- 今日保留结构化快速反馈，并提供“告诉康复助手”；日历可从日期、任务或外部训练记录
  预关联进入对话。未来日期不能补充反馈，离线自然语言只保留未发送草稿。

### 康复助手与私人档案

- `/plan/conversation` 保留连续对话，回答时读取完整当前计划、当前私人档案版本、有效
  稳定记忆、最近 20 轮对话及最近 14 天结构化反馈、确认任务、Garmin/训记与恢复参考。
- 若问题需要更早历史，服务端最多允许一次明确且不超过 30 天的查询；范围不明确时先追问。
- 私人康复档案只保存人工整理的版本化结构；原始 MRI、PDF、笔记全文和本机路径不发送
  给模型。自动记忆只限稳定目标、偏好、时间、器械和训练习惯，可查看、修改和删除。
- 普通对话只读，不能通过记忆或模型输出改写安全规则、档案或计划。自然语言反馈先生成
  可编辑确认卡，使用者确认后才保存，并由确定性规则重新计算红黄绿。

### 动态计划

- 反馈和执行记录保存后，可请求 DeepSeek 生成结构化调整建议。
- 页面展示修改原因、影响日期和任务差异。
- 使用者接受后，软件执行变更并生成新计划版本；拒绝则保留建议与决定。
- 所有历史版本可查看和回滚。

### 出差、突发和暂停

- 可为一个日期范围启用临时执行上下文，并按当天场地、器械和时间条件选择私有
  备选方案。
- Tracker 使用固定的计划时区；当前私人 Tracker 为 `Asia/Shanghai`，短期出差时
  不随设备时区自动改变“今天”或计划生效日期。设备时区不同时，页面明确提示当前
  计划日按上海时间计算。
- 临时选择不自动改写基础计划；记录实际采用的方案、完成情况和身体反馈。
- 返程或暂停结束时，页面回顾最后有效阶段并给出接续差异；确认后才生成新版本。
- 生病、急性症状或红灯不套用普通出差降级流程，而进入停止和重新评估状态。

### 阶段和结项评估

- 计划目标日期只开启评估，不自动判定完成。
- 评估记录目标完成、左右侧反应、力量/动作控制、负荷耐受和下一阶段意向。
- 使用者查看冻结证据后，逐周确认是否计为有效，并人工选择维护、专项进阶、延长
  当前阶段或专业复评；这是使用者决定，不是系统或 AI 的医学判定。
- 中间的出差、暂停和多次降级影响有效训练周，不能只按日历周数推进。
- 红灯只允许进入专业复评；黄灯或缺少完整绿灯证据时不能选择专项进阶。系统不自行
  发明完成率、反馈天数或连续周数阈值，也不会因评估决定自动改写未来计划。

### 提醒与回顾

- 页面内持续显示今日反馈、待同步、待确认和集成失效状态。
- 主屏推送和角标是用户主动授权的可选增强，不是完成核心流程的前提。
- 计划内的近期回顾展示症状、完成、负荷和睡眠背景的时间关系，不生成医疗诊断或因果结论。

## 数据来源与权威边界

| 信息                         | 权威来源                     | AK Tracker 的职责                         |
| ---------------------------- | ---------------------------- | ----------------------------------------- |
| 康复计划和任务               | AK Tracker 计划版本          | 按日期展示、版本化、确认变更和回滚        |
| 跑步等活动与生理指标         | Garmin                       | 同步、标准化并建议关联任务                |
| 力量训练动作、重量、组次次数 | 训记                         | 只读同步、保留来源并建议关联任务          |
| 疼痛、肿胀和主观感受         | 使用者反馈                   | 结构化记录、安全判断和追加历史            |
| 任务是否完成                 | 使用者在 AK Tracker 中的确认 | 保存决定，外部数据和 AI 均不能代替确认    |
| 后续计划调整                 | 使用者确认后的新计划版本     | AI 只提出建议，软件校验并执行已确认的变更 |

训记第一版只接入训练数据，不读取饮食、身体数据或官方计划，也不向训记写回训练。
Garmin 中与膝关节康复无关的日常卡路里、身体成分和完整健康数据不进入本项目。

## 初始数据范围

- 初始计划来自现有康复计划文档，导入时记录来源仓库、路径和提交版本。
- 计划开始日期由使用者确定。
- 第一版不补录开始日期之前的任务执行、身体反馈或 Garmin 历史数据。
- 医疗检查、诊断和医嘱仍以原始笔记为权威来源，不复制到公开代码仓库。

## 成功标准

- iPhone 主屏安装后可快速进入今日页面。
- 离线时仍能查看最近缓存计划并提交待同步记录。
- 重复同步不会产生重复事件或重复 Garmin 记录。
- 同一份训记训练在重复同步或内容更新后不会重复创建；已确认关联的来源发生变化时，
  应提示重新检查，不能静默改写已确认的实际执行。
- AI、Garmin 或 GitHub 任一服务故障时，今日计划与本地记录仍可使用。
- 日期和顶层 Tab 在慢网络下立即响应，未加载数据只影响对应内容区。
- 任何外部活动都不能未经人工确认自动完成计划任务。
- 正常力量训练只需在训记记录一次；AK Tracker 能同步详情并让使用者用一次确认
  完成关联，不再要求逐项重复输入重量和组次。
- 计划调整可解释、可确认、可审计、可回滚。

## 非目标

- 医疗诊断或替代医生意见。
- 自动批准高风险训练调整。
- 汇总与康复无关的全部健康数据。
- 读取训记的饮食、身体数据或官方计划，以及向训记写回训练。
- 依据 Garmin、睡眠或趋势图自动给出医学结论。
- 多用户、公开分享、社区或商业化能力。
