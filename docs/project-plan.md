# 项目计划

## 文档职责

本文是 AK Tracker 的交付与进度权威来源，只记录当前状态、里程碑、依赖、风险和
验收门槛。产品范围见[产品需求](product-requirements.md)，系统怎样运作见
[系统架构总览](architecture/overview.md)，技术决策及原因见[ADR](adr/README.md)。

## 当前状态

状态日期：2026-08-05。

| 范围             | 状态                       | 已完成                                                                                    | 下一缺口                             |
| ---------------- | -------------------------- | ----------------------------------------------------------------------------------------- | ------------------------------------ |
| 部署与身份       | 已可用                     | Vercel、Neon、GitHub OAuth、数字 ID 白名单、部署冒烟                                      | 恢复演练                             |
| 计划与任务       | 重新起算待复核             | 私人不可变计划时间线、按日期有效版本、任务与实际记录                                      | 正式起算版本的生产语义复核           |
| 今日与日历       | 关联反馈返修已验收         | 聚合 API、持久 Tab、外部来源关联反馈与管理层级最小返修                                    | 持续性能与水合稳定性观察             |
| 身体反馈         | 已可用                     | 结构化追加、私人安全策略、全屏流程与离线可靠提交                                          | 24 小时反应和待办状态                |
| 出差、暂停与接续 | 已完成                     | P1b-1/P1b-2/P1b-3 均已验收                                                                | 后续真实使用持续验收                 |
| 离线可靠性       | 代码收口                   | P2a 至 P2b-4 均已验收                                                                     | iPhone 主屏人工发布验收              |
| GitHub 数据镜像  | 开发中                     | 单条消费者、笔记入口、响应后、前台与 Cron 恢复触发                                        | 首条真实写入验收和全量重建           |
| Garmin           | 增强完成                   | 活动、睡眠与步数的 P3b/P5a-2 当前范围均已验收                                             | 真实使用与平台 Cron 持续观察         |
| 训记             | 项目经理已验收             | 加密凭证、明细关联、服务端有界追赶同步与失败恢复；自动/手动同步竞态与错误可见性返修已验收 | 真实私人数据同步由使用者本人受控验证 |
| AI 计划调整      | 增强完成                   | P4b、P5a-1 与 P5a-3 当前范围均已验收                                                      | 使用者本人受控测试 Flash/Pro         |
| 趋势、评估与通知 | 核心完成                   | P4a、P4b、P4c-1、P4c-2a、P4c-2b 均已验收                                                  | 明确私人规则后的可选增强与持续观察   |
| UI/UX            | UI-R3b 真机待验            | UI-R1/UI-R2 降噪与根 Tab 导航竞态均已验收                                                 | iOS 首帧真机最终验收                 |
| P5 用户追加增强  | 返修完成、等待项目经理复核 | P5c 计划入口、康复助手、档案/记忆、文本反馈与 Context v3                                  | 项目经理复核及使用者受控端到端验收   |

“基础可用”只表示当前私人主流程能够使用，不代表已经达到离线、性能、故障恢复和发布门禁。
P0-P4 核心首版完成度保持 100%；使用者本轮追加的 P5a-1、P5a-2 与 P5a-3 指定范围
也已 100% 完成并通过项目经理验收，不改变核心里程碑。使用者随后追加的 P5b-1
代码与只读界面范围也已由项目经理验收，不倒退 P0-P4 或 P5a 的既有完成状态；真实
Provider 端到端操作仍由使用者受控执行。

UI-R1 已由项目经理独立验收：今日与设置遵循默认正常态隐式、异常态显式；今日一级聚焦
任务与身体反馈，设置一级改为分组详情入口。Garmin、训记、DeepSeek、GitHub 备份、本机
数据和账号的已有能力完整迁入各自详情页；设置详情返回、浏览器后退/前进、URL 和持久 Tab
均已复核。日历和趋势的视觉整理明确留给 UI-R2。本切片没有改变领域模型、计划起算、离线
命令、安全判断或 Provider 契约。

UI-R2 已于 2026-08-02 由项目经理独立验收：趋势的空/部分/完整数据语义，以及日历主体和
图例降噪均通过。独立生产复核同时确认：今天不显示“回到今天”或“退出”；选中
`2026-08-03` 后仅出现“回到今天”，返回后 URL 恢复为 `/calendar?date=2026-08-02`，今天
日期格恢复焦点和 `aria-pressed=true`，两项冗余按钮均消失。UI-R1 与 UI-R2 组成的本轮 UI
信息降噪专项至此 100% 完成。此专项未改变领域模型、计划、安全、离线、Provider、根 Tab、
URL/history、草稿或缓存契约。

UI-R3b 的 iOS 首帧代码已完成，仍待使用者真机最终验收：在 iOS 主屏/PWA 首次进入时会出现
fixed-bottom 视觉锚点漂移的风险，受保护 App Shell 已改为动态视口 grid 壳、
内部内容滚动和正常布局行底栏；安全区仅在导航内部应用。匿名浏览器门禁在 320/375/390/
393/430 宽度保存未滚动首帧与真实滚动后的导航截图并逐像素比对，同时回归键盘高度、旋转和
恢复后的导航位置。CSSOM 几何值作为辅助，不单独充当 WebKit 绘制通过证据；真机首次打开仍
需独立验收。

同一切片还收口 Garmin 设置详情：已连接时先显示活动与睡眠/步数同步，Token 替换降为默认
关闭的“连接维护”；单日同步与追赶批次说明范围、结果和继续位置，最近成功处理日期不再暗示
一定读取到活动。已有 DTO 支持的新增/更新/未变化结果会显示；历史追赶起点没有 Provider-
neutral 字段，未在 UI 中猜测。Garmin、训记和 DeepSeek 的 disabled 按钮均以独立颜色、边框、
文字和 `not-allowed` 指针区分 native disabled 语义。计划起点日仅在 DTO 的
`effectiveFrom === targetDate` 且当天无任务时提示“第 1 周从今天开始／恢复或基线日”，普通
休息日不改变。此切片不改变领域模型、计划起算、安全、离线命令或 Provider 契约。

UI-R3b 根 Tab 时序返修已于 2026-08-04 由项目经理独立生产复核并验收：设置详情 Link 的 RSC 导航尚未提交时，
后点的今日、日历或趋势会立即成为 URL、`aria-current` 与可见面板的共同事实；根壳通过导航代次
忽略迟到的详情 pathname/children。尚未写入详情 URL 的 pending 窗口由 History 代次压过，
已加载或进入 commit 的详情只发起一次 App Router 导航，以保留正确的 back/forward tree。详情 URL 不再污染设置
根 Tab 的缓存回退地址；根 Tab 间仍使用既有快速 History 路径，日历日期 query、持久 DOM、草稿、
滚动及 back/forward 契约不变。匿名 production-build 门禁覆盖 Garmin、历史补录、DeepSeek、
账号四个详情、320/375/390/430px、不同延迟、详情错误态和 20 次快速连续点击。生产长 history
链进一步发现 pathname 与迟到根 children 可能交叉提交；四个根面板现只由持久 Host 内对应的
客户端组件渲染，App Router children 仅用于非根详情，从结构上移除把日历内容绑定进设置面板
的入口。独立验收重放账号详情→日历→back→forward→设置长链，最终 URL、设置 active、7 行
入口与可见面板一致，日历和账号详情均无残留；Garmin→今日、历史补录→日历、DeepSeek→计划
三条未等待详情稳定的快速竞态路径也均由最后一次根 Tab 点击获胜。生产控制台无 warning/error，
全程未调用 Provider、未写业务数据。

正式康复计划重新起算切片当前待复核：旧计划版本与计划前历史事实继续保留；新的
不可变计划版本按既有周结构重新生成，并通过可审计、幂等的导入路径同步 Tracker 正式
起点与未来任务投影。本切片不调整私人处方内容，不调用 AI，也不把近期私人训练细节
写入公共仓库。

## 里程碑

### P0：交互与数据基础

目标：界面交互不再依赖每次服务器往返，并建立后续离线和多页面能力的稳定底座。

P0 按三个可独立上线和验证的切片实施，不把正确性、性能和视觉改造塞进一次发布：

#### P0a：安全与数据正确性

状态：当前实现边界已完成并通过数据库集成验证；P0-10 已随首个训记只读 Provider
边界完成故障隔离验证。

- 移除 Service Worker 对认证首页的缓存，并验证退出后无法读取私人内容。
- 按目标日期和固定计划时区解析有效计划版本。
- 客户端生成稳定命令 ID；任务、事件和 outbox 使用 Neon HTTP 非交互事务原子写入。
- GitHub OAuth 白名单从可变 login 迁移为不可变数字用户 ID。
- 生产 `0003` migration journal 的 `created_at/hash` 已与本地 Drizzle migration 核对；
  后续使用只读 `db:journal:check` 作为发布证据。
- 已交付 P0-01、P0-02、P0-03、P0-07、P0-08、P0-09 和 P0-11。
- P0-10 已验证：已提交核心命令后，后续独立的训记同步请求即使遇到 Adapter 限流，
  也只更新集成状态，不回滚或污染任务、事件和 outbox。该结论不包含同一请求内的
  响应后消费者；后续 Provider 仍需各自补故障测试。

#### P0b：导航与客户端数据

状态：原实现已完成正确性验收，但 2026-07-23 的真实线上试用再次发现顶层 Tab 仍受
动态 RSC 往返和全页过渡影响，因此重开为 P0b-r1 性能返修。P0b-r1a 已完成生产验证并
由项目经理验收。使用者确认已访问 Tab 切换已无等待，首次进入仍偏慢但当前不阻塞
使用；P0b-r1b 只保留不扩张范围的最小查询波次补丁，更多性能改造降为观察项。正式
migration、私人策略和聚合 API 结论保持有效；不能再用“100 ms 内显示加载框”代替
缓存内容可见。

- 建立共享受保护 App Shell，四个顶层 Tab 保持挂载和状态。
- 接入 TanStack Query、稳定 Query Key、聚合 API 和精确缓存失效。
- 日期和 Tab 立即响应，详情区独立加载，迟到响应不能覆盖最新选择。
- `TrackerSafetyPolicy` 已迁移到不可变 PostgreSQL 版本；服务端按发生时间解析，聚合
  DTO 鉴权下发，事件记录实际使用的版本/hash。
- 交付 P0-04、P0-05 和 P0-06。

#### P0b-r1a：客户端导航与部署区域返修

状态：已于 2026-07-24 由项目经理完成登录态生产复核并验收。

- 生产基线：今日首次进入日历约 3.1 秒；已访问 Tab 的返回仍约 0.34–1.59 秒。
- 根 Tab 使用受保护 Layout 中的持久客户端 Host；访问过的页面由 React Activity 隐藏
  而不卸载，表单草稿、展开状态和各 Tab 滚动位置继续保留。
- 根 Tab 使用 Next.js 支持的 History API 同步 URL，不再通过动态 RSC 请求完成每次
  切换；直接 URL、服务端鉴权、反馈拦截路由和非根子页面仍保留原路由语义。
- 设置页改为客户端 Query 边界，页面壳不等待集成或镜像状态；今日数据成功后在浏览器
  空闲时精确预取当月、当天和设置状态。
- Vercel Functions 单一区域从默认 `iad1` 调整为 `sin1`，与 Neon 新加坡区域对齐；
  必须以生产构建配置和响应证据确认生效。
- 新门禁要求：未缓存 Tab 在 100 ms 内显示稳定目标壳；缓存 Tab 在 100 ms 内显示真实
  内容，不显示全页 loading，也不重新请求聚合 API。发布后在同一生产环境至少测量五次
  冷/暖路径并报告中位数和最差值。
- 若首次数据可见中位数仍高于 1.5 秒，r1a 不宣称性能收口；下一步只进入 P0b-r1b 的
  PostgreSQL 聚合查询波次优化。
- 正式域名验证覆盖：日历日期查询参数、日历与设置往返、刷新、今日与趋势往返以及
  浏览器前进/后退；各步骤的 URL、可见内容和 `aria-current` 均保持一致，日历选择与
  详情状态能够恢复，登录后控制台无 warning/error。
- 首次稳定壳与已访问页面返回的断裂体验已得到结构性修复。使用者后续确认 Tab 切换
  已无等待，并同意把首次进入速度降为观察项；因此 r1b 只做最小查询波次收口。

#### P0b-r1b：首次数据查询波次最小收口

状态：最小补丁已于 2026-07-24 由项目经理验收；不继续扩大性能范围，不把加载文案
或动画当作数据性能改善。

- 今日聚合在一次查询中取得 Tracker 与目标日期的有效计划版本，减少重复的前置
  PostgreSQL 往返。
- 取得必要上下文后，并行读取当天任务/反馈、安全策略与执行状态；执行状态中的独立
  上下文读取也并行启动。
- 日历聚合在计划版本确定后，并行读取任务、反馈与暂停状态，减少无依赖的串行波次。
- 新增查询编排测试，锁定无依赖读取同时启动；没有修改数据库 Schema、身份门禁、
  计划时间线、安全策略或离线边界。
- 首次真实数据可见速度继续作为体验观察项。只有再次成为使用阻塞时，才重新按鉴权、
  网络、函数、数据库和客户端分段测量，不预先扩张到更多查询重构。

#### P0c：核心界面落地

状态：P0c-1“设计系统基础与今日页任务卡纵向切片”、P0c-2“身体反馈全屏流程”和
P0c-3“日历视觉纵向切片”均已由项目经理验收，P0 核心实现完成。iPhone 长期体验
继续作为持续验收，不阻塞进入 P1。

- 在训记只读纵向切片确认实际训练信息结构后实施，避免把重复手工录入固化为正式
  任务卡。
- 落实已确认的 UI/UX Token、任务卡、反馈卡、日历和状态语言。
- 身体反馈使用稳定 URL 和独立全屏流程；提交成功停留在结果状态，失败保留草稿和
  幂等命令，软导航返回时保留今日任务草稿与滚动上下文。
- 对今日、日历和登录/退出路径完成手机尺寸浏览器测试与 iPhone 真机验收。

#### P0c-r1：面向使用者的文案与信息降噪

状态：已于 2026-07-24 完成生产复核并由项目经理验收。

- 今日、任务展开、身体反馈、日历、设置、待同步、临时训练安排、暂停、接续、离线
  公共壳、趋势和错误/空状态统一改为面向行动的用户语言。
- 默认页面只保留会影响下一步的训练、安全、未保存、离线和待处理事实；架构保证、
  验收结论与开发术语从正常页面移除，异常说明仍给出具体处理动作。
- 训记连接明确使用“训记 API Key”，GitHub 区域使用“数据备份”，本机区域说明实际
  保存内容；没有改变凭证、安全判定、同步状态或数据边界。
- 自动化门禁同时验证正常界面不出现内部术语，并保留疼痛量表、红灯处置、未保存、
  仅本机、清除本机数据等必要说明。

退出条件：当前具备实现边界的 P0 用例全部通过；慢网下日期和 Tab 在 100 ms 内显示
本地选中状态或稳定页面框架。P0-10 已在首个真实外部消费者边界通过；新增 Provider
仍需各自满足相同故障隔离门禁。

### P1：当前康复闭环

目标：覆盖日常执行、出差、暂停和恢复训练的真实使用过程。

#### P1a：训记只读训练输入

状态：P1a-1“只读连接与单日同步底座”、P1a-2“单日训练明细与人工关联闭环”和
P1a-3“计划开始日至今天的追赶同步”均已由项目经理验收，P1a 实现阶段完成。真实
训记 Key 和真实私人数据试用留待使用者以后自行轮换 Key 后进行，不是 P0c 的发布
前置条件。

本次训记单日归属、HTTP 200 业务错误兼容与同日零时间草稿阻断修复已由项目经理验收。Production 上一次受控“同步到今天”已成功处理 2026-08-04 至 2026-08-06，3 天成功、0 天失败且发现训练记录；日历 2026-08-05 显示 1 条训记实际训练和同日 Garmin 来源，0/0 草稿未显示；未修改任务关联或完成状态。

- 设置页录入轮换后的训记 API Key；服务端验证、认证加密保存并且不回传原值。
- 只调用训练读取接口，按日期同步计划开始日以来的力量训练明细；不接饮食、身体
  数据、官方计划或写回。
- 追赶范围只由服务端依据 Tracker 开始日期、计划时区今天、逐日同步状态和持久游标
  计算；每个请求只处理有界日期批次，设置页在打开期间继续调用，中断后从数据库
  状态恢复。客户端不提交起始日期，失败日期不回滚已成功日期。
- 按稳定来源 ID 和内容 hash 幂等 upsert，重复同步不产生重复记录；来源变化进入
  待复核。
- 今日任务展示训记明细和关联建议，使用者确认、修改或取消关联；任务完成选框保持
  独立，手工训练输入退为兜底。
- 为后续 Garmin 融合保留多来源关联模型，但本切片不同时实现 Garmin 运行时接入。

退出条件：S13 通过；能够从设置页连接、同步一天的匿名训练夹具并确认关联；Provider
认证、限流或超时不影响今日计划、反馈和手工兜底。生产验证只做只读同步，不用真实
训练内容作为测试产物。

#### P1a-r1：外部活动关联反馈与信息层级返修

状态：已于 2026-08-05 由项目经理完成独立复核并验收。

- 今日只突出尚未处理、建议关联或来源更新后需要复核的记录；已标记与计划无关的记录
  退出今日主流程，稳定确认关联只在对应任务下保留轻量来源数量与日历入口。
- 日历当天继续保留完整来源事实；稳定确认关联和与计划无关默认使用紧凑摘要，使用者
  明确点击“修改关联”后才展开任务选择、更换关联和与计划无关操作。
- PUT 权威响应统一投影同日 Today、day 与私有离线快照，并精确失效当月摘要；409 会
  重新读取同日权威数据，使新的 `sourceVersion` 和 `needsReview` 再次进入显式待处理。
- 关联决定继续与任务完成状态完全分离；没有修改数据库 Schema、迁移、领域关联语义或
  Provider 合约。生产复核只读，不调用真实 Garmin、训记或 DeepSeek，不创建或修改业务记录。

独立复核确认 `HEAD=origin/main=8f005332` 且工作区干净；定向 Vitest 3 个文件、19 个测试和
typecheck 通过。Production 登录态只读检查确认 2026-08-04 的步行默认紧凑显示“已标记为
与计划无关”，跑步默认紧凑显示“已关联：第 1 周 · 短跑／跑走训练”，两者默认均只有
“修改关联”，不渲染任务选择控件；Calendar→Today→Calendar 后 URL 恢复为
`/calendar?date=2026-08-04`，两个紧凑状态保持，Today 不出现该历史日旧大卡。复核没有
执行任何真实关联写入。

- 计划内与计划外训练使用统一实际记录模型。
- 每日、训练后、次日和突发反馈可独立追加。
- 出差/器械受限执行上下文、当天条件与私有备选方案。
- 暂停、返程接续和未来任务差异预览。
- 当前私人计划的新版本导入与历史日期验证。

退出条件：测试场景 S01、S02、S06、S07 通过；临时执行条件不制造计划版本，真正
改变未来安排时才由使用者确认新版本。

#### P1b：临时执行上下文

状态：P1b-1“出差执行上下文与当天条件纵向切片”、P1b-2“暂停状态与暂停日语义”
和 P1b-3“返程/暂停后的接续预览与人工确认”均已由项目经理验收，P1 当前实现阶段
收口。

- 创建或结束出差、器械受限日期范围；计划日始终按 Tracker 的固定计划时区解释。
- 今日页同时展示基础计划与当前上下文，当天条件和私人备选方案可以逐日独立选择。
- 上下文、当天决定、事件和 outbox 原子提交且幂等，不改变计划版本或任务完成状态。
- 私人备选处方只存 PostgreSQL 和私有数据仓库；公共层只包含中性 Schema、导入器、
  鉴权投影和匿名测试。
- 红灯、生病和急性症状不进入普通降级方案，统一显示停止和重新评估提示。
- 暂停作为高于出差/器械受限的独立覆盖层；开始与结束均通过稳定命令、事件和 outbox
  原子审计。暂停日保留基础计划和任务状态，结束后只进入待接续评估。

P1b-1 退出条件：S06 的范围、逐日选择、计划不可变、原子审计、移动布局和安全分流
通过自动化与生产只读验收。后续 P1b 切片再处理暂停、返程和接续差异。

P1b-2 退出条件：S07 前半段的暂停开始/结束、同日执行上下文优先级、计划和任务
不可变、暂停日历语义、原子审计及故障重试通过自动化与数据库集成验证。结束暂停仅
进入待接续评估，返程和接续差异留待后续切片。

P1b-3 交付状态：结束执行上下文或暂停会原子创建待接续评估；今日页和稳定 URL
展示中断摘要、最后确认训练与未来日期差异。按原计划继续只记录决定；顺延只复制
生效日起尚未执行的未来任务到新的不可变计划版本。基线计划变化会让旧评估过期并
基于最新版本重建；评估同时冻结创建时的计划时间线头，任何后来新增的计划版本都会
使旧评估过期。创建评估时已有后续版本则保留“按原计划继续”，禁用不安全的顺延。
所有决定使用稳定命令、事件、唯一决策锁和 outbox 原子审计。正式 `0009`/`0010`
migration、临时 Neon 分支和 S07 后半段集成验证已完成并通过验收。

### P2：离线可靠性

目标：在 iPhone 主屏模式和不稳定网络下不丢计划、训练或反馈。

#### P2a：私人离线只读快照与身份隔离

状态：已由项目经理验收。已交付今日、日历月/日详情及最小安全策略引用的白名单
只读快照；任务和反馈的离线写入由后续 P2b-1 在此基础上扩展。

- Dexie v2 以不可变 GitHub 数字用户 ID、Tracker、查询种类和日期范围隔离快照；
  过期、损坏和其他身份的数据不会恢复。
- 在线鉴权查询成功后保存经过 Schema 白名单投影的数据；完整安全规则、集成凭证、
  Provider raw payload、Cookie 和 Authorization 不进入 IndexedDB。
- 已加载的应用由 TanStack Query 恢复快照；销毁旧 document 后，固定公共离线壳可在
  无 session/RSC/动态 chunk 时读取相同快照，显示今日、月摘要和单日详情的缓存时间与
  只读状态。P2a 验收时所有写操作均要求联网；P2b-1 只放开任务和反馈两类核心命令。
- 退出、身份变化和设置页“清除本机数据”共同清理 Query Cache 与私人 IndexedDB；
  Service Worker 只缓存固定公共离线壳和公共静态资源；认证页面、RSC 和 API 不进入
  Cache Storage。Chromium browser-context 已覆盖断网、关闭页面、从 `/` 冷启动和
  日历浏览；iPhone 主屏 PWA 真机飞行模式仍待验收。
- 公共壳仅在浏览器确实离线时读取快照；在线直达会返回受保护路径重新鉴权。独立最小
  显示契约按 kind 校验身份、Tracker、日期与所有展示字段，损坏的今日、月或单日
  快照分别进入安全空态，不会显示成零任务或零反馈。

#### P2b-1：核心离线命令队列与可靠重放

状态：已由项目经理验收；不据此提前声明 P2b 或 S05 全部完成。

- Dexie v3 将 `pendingCommands` 启用为版本化本机 outbox；只接受任务状态/实际训练
  更新和身体反馈追加，按数字身份与 Tracker 隔离，不保存凭证或 Provider raw payload。
- 在线和离线提交都先落盘，再用确定性投影叠加到今日、单日、月历和公共离线壳；基础
  快照保持服务端权威。反馈只追加，任务连续编辑按创建顺序重放。
- 应用启动、恢复联网、重新可见、窗口聚焦和手动重试共用单消费者租约；相同命令 ID
  依赖服务端幂等，网络/超时/5xx 退避，鉴权和业务冲突进入明确待处理状态。
- 匹配的私人安全策略按身份、版本和 hash 白名单保存；离线反馈只显示本机预判，缺少
  匹配规则时不伪造绿灯，服务端重算仍是最终权威。
- 服务端规范成功结果先更新 Query Cache 与 IndexedDB 快照，再删除本机命令；退出、
  身份变化和清除本机数据会清理全部私人状态，存在未同步记录时先明确二次确认。
- 任务与反馈之外的来源关联、执行上下文、暂停和接续继续要求联网；复杂冲突处理交互
  和公共离线壳中的新增编辑入口不在本切片。

#### P2b-2：待同步状态中心与队首人工处置

状态：已由项目经理验收。

- 设置页提供当前身份与 Tracker 的待同步入口；状态页按稳定队列顺序显示类型、计划
  日期、发生时间、安全状态和最小可读摘要，并明确队首与被阻塞记录。
- `retryable`、`waiting_auth` 和尚未发送的队首只显示实际可执行的重试；同步中的命令
  防止重复操作，`needs_attention` 不允许原命令直接重试。
- 冲突队首可在二次确认后放弃。操作必须在线并先取得今日、单日和月历权威 DTO；随后
  在一个本机事务中替换快照、只删除队首，再由权威数据和剩余命令重新投影并续播。
- 非队首始终只读；离线、取消确认、权威数据读取失败或并发重复处置都不会删除记录。
  错误只显示安全解释，不展示服务器原文、内部 ID 或完整健康内容。
- 本切片不提供“按当前状态重新提交”，也不扩展其他领域命令或公共离线壳编辑能力。

#### P2b-3a：公共离线冷启动壳的今日任务状态写入

状态：已由项目经理验收。

- 只允许同一数字身份、`knee-rehab`、未过期今日快照中的既有任务在公共离线壳切换
  `planned` 与 `completed`；不提供反馈、跳过、训练明细或其他领域写入。
- 每次操作先把独立的版本化命令原子写入 Dexie v3，再重新投影页面；连续切换保持有序
  命令历史，不做本机合并。
- 公共壳不直接调用写 API，恢复联网后返回受保护应用重新鉴权，并由既有重放器继续处理。

#### P2b-3b：公共离线冷启动壳的身体反馈追加

状态：已由项目经理验收。

- 只在同一数字身份、`knee-rehab`、未过期当前计划日快照通过严格校验时提供完整身体
  反馈表单；其他身份、Tracker、日期或损坏快照保持只读。
- 每次提交先把独立反馈命令原子写入 Dexie v3，再更新本机反馈计数与结果；失败保留
  完整草稿和同一命令 ID，继续添加则创建新的追加式命令。
- 公共壳不加载或解释私人安全策略；反馈固定等待联网后由服务器权威判断，不在离线
  状态显示绿灯、黄灯、红灯或训练升级建议。

#### P2b-4：PWA 新版本提示与受控更新

状态：已由项目经理验收。P2 代码实现已收口；iPhone 主屏飞行模式、恢复联网和真实
worker 更新仍是人工发布验收项，不据自动化结果虚报完成。

- 新 Service Worker 只在使用者确认后接管当前页面，不在安装完成时强制刷新。
- 受保护 App Shell 显示克制的版本提示，并区分稍后处理与立即更新。
- 更新不清理 IndexedDB、本机待同步命令、私人快照或安全策略；离线公共壳继续稳定
  工作。

#### P2b 后续收口

- 更复杂的冲突解释、人工取舍和其他领域命令类型。
- 离线壳中的新增编辑入口，以及需要跨来源版本协调的领域命令。
- iPhone 主屏模式下的飞行模式、重启、恢复联网和版本更新真机验收。

退出条件：S05、S11 通过；完成一次 iPhone 飞行模式、重启、恢复联网和重复同步的
真机验证。

### P3：外部数据与镜像

目标：建立可恢复、可替换、不会阻塞核心闭环的外部数据链路。

#### P3a-1：GitHub 私有镜像单条消费者与状态底座

状态：代码、正式 migration、安全状态、临时 Neon 租约验证和本地门禁已由项目经理
验收。Production Secret 已配置，生产状态端点已验证为 `configured`；当前 outbox 为
0，因此真实单条私仓写入、相同内容不产生无意义提交、`lastSucceededAt` 和生产计数
仍待下一条真实业务记录产生后受控验收，本切片尚未记为端到端完成。

- 使用服务端 GitHub Adapter 和现有持久 outbox，把安全目标路径的一份结构化 JSON
  幂等写入独立数据私仓。
- 提供租约、过期恢复、有限重试、权限错误隔离、鉴权状态 DTO 和手动小批量入口。
- 本切片不实施笔记仓入口、全量重建、Garmin、AI 或趋势。

#### P3a-2：个人笔记稳定追踪入口

状态：已由项目经理验收。远端入口与 iCloud 外干净副本均已验证；旧 iCloud 目录
保持不动。

- 入口链接独立数据私仓根目录与 `trackers/knee-rehab/`，并说明联合读取顺序。
- 两个私仓继续独立授权；不复制追踪数据，不使用 Git submodule，不允许应用自动改写
  笔记仓库。
- Neon PostgreSQL 仍是运行时权威数据源，GitHub 数据私仓仍是可能短暂滞后的异步
  结构化镜像。

#### P3a-3a：核心写入后的尽力单条镜像消费

状态：已由项目经理验收。

- 任务、反馈、执行上下文、暂停、接续和来源关联命令成功后，使用 Next.js `after()`
  在响应完成后最多消费一条既有 outbox。
- 复用 P3a-1 的消费者、租约、同路径串行、退避、状态与 GitHub Adapter；配置缺失
  或无效时安全跳过。
- 调度或消费失败不改变核心写入响应，不新增第二套队列，不记录凭证或 Provider 原始
  错误。

#### P3a-3b：应用打开与恢复联网后的镜像修复触发

状态：已由项目经理验收。

- 只在受保护应用首次在线打开与离线恢复 `online` 后调用现有鉴权同步端点；同一页面
  使用一分钟节流与运行中合并，路由切换、Query refetch、普通焦点和可见性变化不触发。
- 离线安全跳过；401/403 在当前壳生命周期内停止自动触发；网络、服务端或响应校验
  失败不阻塞 App Shell、导航与草稿。
- 只有成功且通过公共 Schema 校验的权威响应才更新唯一的 GitHub 镜像状态 Query
  cache；设置页镜像卡订阅同一 Query key，不伪报成功。

#### P3a-3c：每日 Cron 镜像修复兜底

状态：代码、生产 `CRON_SECRET`、平台注册及首次平台实际调用均已验证。Vercel 于
2026-07-23 19:53:53 UTC 调用 Production `GET /api/cron/github-mirror` 并返回 200；
只读数据库复核显示 pending/processing/failed 均为 0、`lastSucceededAt` 为空，因此
本次为真实的空队列 idle 成功，没有制造镜像写入。

- `vercel.json` 每日一次调用独立 Route Handler；计划时间使用 UTC，Hobby 套餐允许
  小时级漂移，不承诺精确分钟。
- Route Handler 只接受与服务端 `CRON_SECRET` 严格匹配的 Bearer 请求，不使用用户
  Session；缺失或错误配置在接触 outbox 前失败关闭。
- 每次只复用既有消费者处理最多三条、运行最多八秒；租约、同路径串行、退避和幂等
  继续由持久 outbox 负责。单次失败不在 Cron 内循环重试，也不影响核心业务事实。
- 响应只包含安全状态和计数；异常或非法结果收敛为通用不可用状态，不记录或回传
  Authorization、目标 payload、Token 或 Provider 原始错误。

#### P3b-1：Garmin 私人只读连接的运行时与凭证可行性

状态：代码、匿名 Preview 运行时和安全边界已于 2026-07-24 由项目经理验收。该阶段的
部署 Spike 只使用匿名夹具；后续受控生产验证事实见 P3b-2a。

- Garmin 官方 Developer Program 当前只面向业务使用，Activity API 只有在申请获批后
  才提供评估环境；当前项目未把官方 API 表述为已连接。
- server-only `GarminClient` 明确活动、基础步数与睡眠证据边界；Garmin 数据不携带
  任务完成状态，训记继续负责力量训练重量和组次。
- 私人读取候选固定为 `python-garminconnect==0.3.6`，采用受信任本机认证/MFA 后导入
  token bundle 的路径；云端不保存 Garmin 密码。
- Token 使用严格版本化凭证信封和既有 AES-256-GCM、随机 nonce、key version、
  provider AAD，不进入浏览器 DTO、日志、构建、镜像或 Git 历史。
- Vercel Preview `dpl_APHB4xwbBJw8f4zcKQgzmxETb5pW` 真实验证 Python 3.12.13、固定
  客户端、`curl_cffi`、匿名 Token 解析与安全错误分类均可构建和启动；临时验证 Route
  已删除，不进入 Production。
- Garmin 官方 JavaScript FIT SDK 作为无凭证活动导入兜底；是否先做 token-only 单日
  读取，还是先做 FIT 导入，由项目经理在 P3b-2 前决定。项目经理已决定 P3b-2 优先
  验证 token-only 单日自动读取，FIT 导入继续作为失败兜底。

P3b-1 的匿名 Spike 不等于 Garmin 已连接，也不授权扩展历史追赶同步。长期 Token
生命周期、增量同步、限流与故障恢复仍需在后续切片验证。

#### P3b-2a：Token-only 单日活动受控验证

状态：代码、匿名测试、Preview 与 Production 安全部署已由项目经理验收；中国区真实
Token 导入、刷新、Node 到 Python Runtime 及生产单日白名单活动摘要链路已完成受控
验证。使用者也已完成计划开始日前历史日期的生产复验，P3b-2a 验收完成；公共文档不记录
具体日期、活动类型或私人活动内容。

- 本机授权助手在使用者 Mac 的临时 Python 环境中处理账号、密码与 MFA，只生成固定
  `python-garminconnect==0.3.6` 的严格 token 文件；密码不进入网页、Vercel 或聊天。
- 本机授权与 Vercel Runtime 使用职责分离的固定依赖清单：本机 Python 3.12–3.14
  始终使用已验证的 `cffi 2.0.0`，Vercel 生产锁始终使用 `cffi 2.1.0`；不能再通过
  Python 版本猜测运行环境，Garmin 客户端版本不漂移。
- 设置页只允许导入该严格文件。后端结构校验后复用 AES-256-GCM、随机 nonce、版本和
  provider AAD 加密保存；浏览器只读取连接状态，不能取回 Token。
- 单日预览使用独立 Secret 鉴权的 Vercel Python Runtime，只读取活动时间、类型、时长、
  距离、配速和平均心率，并向页面返回严格白名单摘要。活动明细尚不写 PostgreSQL，
  也不能修改任务状态。
- Node 到 Python 的内部请求具有固定版本、请求/响应大小限制和短超时；认证、限流、
  超时、非法响应及不可用均收敛为安全错误，不返回 Provider 原文。
- 用户明确选择的过去日期允许进行不持久化的单日只读预览，即使早于计划开始日；未来
  日期仍在调用 Provider 前拒绝。同步和历史回填仍受原有计划范围约束，不因本修复扩大。
- Preview `dpl_FzhmN4y1aDPnCJvQYVLoxCbZHzeE` 已确认正式 Python Route 与锁定依赖在
  Python 3.12 构建成功；无内部 Secret 的请求由 Runtime 自身返回 401。该证据没有调用
  Garmin，也不能代替真实 Token 验证。
- Production `dpl_3ocqbQM9zLKxeh7PneCgnUiivaQS` 已 Ready 并绑定正式域名；健康检查为
  `database=ok`，未登录导入/预览接口和未携带内部 Secret 的 Runtime 均按设计拒绝。
  生产门禁同样没有导入 Token、调用 Garmin 或持久化活动。
- 设置页 Garmin 与训记卡片已完成 320/375/390/430 px 移动端收口；活动类型使用中文
  用户文案，未知类型使用中性名称，不显示 Provider 内部标识。该显示切片已由项目经理
  验收。

#### P3b-2b：Garmin 单日活动持久化与人工关联

状态：代码、匿名测试、临时 Neon 集成、移动端门禁和生产只读检查均已由项目经理验收。

- 用户在设置页明确选择一天后，复用 token-only Runtime 和通用日期同步底座读取活动，
  以来源和稳定来源标识幂等写入 `external_records`；30 秒内重复操作复用成功
  状态，不产生重复记录。
- PostgreSQL 文档只保存活动类型、真实开始时间、时长、距离、配速和平均心率白名单；
  Token、Provider raw payload 和内部 ID 不进入浏览器 DTO、日志或公共文档。
- 来源内容变化会递增 `sourceVersion`、更新内容 hash，并把既有人工关联标记为需要
  复核；历史任务状态与已确认事实不被静默改写。
- 日聚合 DTO 使用向后兼容的 Provider 判别联合：训记继续展示力量动作、重量和组次，
  Garmin 以独立活动摘要展示。两种来源可以在同一天并存。
- Garmin 复用现有通用关联命令，允许关联、更换任务或标记与计划无关；建议只在同一
  Tracker、同一天且唯一明确匹配时出现。关联不改变任务完成状态。
- 本切片只提供人工单日同步；定时同步、睡眠、步数和自动关联均不在范围内。

#### P3b-2c：Garmin 有界追赶与两天重叠增量同步

状态：代码、匿名测试、临时 Neon 集成、移动端门禁和生产只读检查均已由项目经理验收。

- 首次追赶由服务端依据 Tracker 开始日和计划时区今天计算，客户端不能提交或扩大
  起点；用户明确选择的历史单日同步继续保持独立。
- 每次请求最多处理五天并持久化下一日期。某日失败立即停批，失败日作为下次起点；
  已成功日期不回滚，中断后可从数据库继续。
- 首次覆盖完成后，从最近成功日期向前重叠两天同步至今天，复用来源 hash、版本和
  `needsReview` 语义识别近期活动修改。
- 设置页每次只发起一个有界批次，显示本批范围、已处理日期、下一日期、最近成功和
  安全错误提示；继续或重试均由用户明确触发。
- Garmin 刷新凭证只更新加密 Token，不重置追赶游标；首次导入或替换凭证仍使用原有
  受控重置边界。

#### P3b-2d：Garmin 前台自动恢复

状态：已由项目经理验收。

- 已鉴权 App Shell 仅在首次在线打开和离线恢复联网时，发起一次无可见阻塞的恢复请求；
  普通聚焦、可见性变化、路由切换和 Query refetch 不触发。
- 自动恢复复用 P3b-2c 的服务端有界批次、持久游标、日期状态和来源幂等；客户端不
  传入起点、重叠窗口或批次大小，也不会自动循环追赶。
- 服务端使用 `integration_sync_state` 原子领取：自动尝试最短间隔为 30 分钟，运行中
  领取两分钟后可过期恢复。手工同步入口不受自动到期判断限制。
- 未连接、待验证、凭证失效或无效状态不会调用 Provider；401/403 和 `needs_refresh`
  会停止当前 App Shell 生命周期的后续自动尝试，人工处理仍留在设置页。
- 只有通过白名单 Schema 的响应才更新 Garmin 状态，并精确失效本批成功日期及月份；
  网络、限流、超时和非法响应不会重建 App Shell、清空草稿、关联任务或改变任务状态。

#### P3b-2e：Garmin 每日恢复 Cron

状态：代码、匿名测试、临时 Neon 并发验证、生产部署和 Cron 注册已由项目经理验收；
首次平台自动调用仍待观察，不能用未授权探针或人工携带 Secret 的请求替代。Garmin
activity 当前实现阶段至此收口。

- `vercel.json` 保留 GitHub 镜像 Cron，并为 Garmin activity 增加独立的每日 GET
  Route。两者位于不同 UTC 小时，不合并成一个调度任务。
- Route 在任何数据库、集成状态、凭证或 Provider 读取前校验 `CRON_SECRET`，不使用
  用户 Session；返回只包含跳过原因或本批计数、游标和安全错误代码。
- Cron 复用 P3b-2d 的 30 分钟到期判断、两分钟原子租约和 P3b-2c 的持久游标；与前台
  恢复重叠时只允许一个实际运行。
- Cron 固定处理 `knee-rehab` 的 activity，每次最多三天，Route 总时限为 45 秒；
  不在单次调用中循环重试，也不读取睡眠、步数或 daily 数据。
- Hobby Cron 只保证每日一次和小时级精度，不能被视为精确分钟调度或队列；失败日仍由
  PostgreSQL 日期状态与游标保留，等待后续前台、手工或下一次 Cron 恢复。

#### P3 后续范围

- GitHub outbox 消费、重试、权限错误、状态展示和全量镜像重建。
- Garmin 长期 Token 生命周期、增量同步和 FIT 文件导入。
- 外部活动与任务的建议关联、人工确认和取消。
- 同步监控：最近尝试、最近成功、游标、错误代码和最老待处理任务。

退出条件：S09、S10 通过；Garmin 重叠窗口无重复，GitHub 故障不影响核心写入，
PostgreSQL 可以重新生成私有仓库镜像。

### P4：AI、趋势与评估

目标：形成可解释、可确认、可审计和可回滚的计划调整闭环。

状态：核心首版已完成并通过项目经理验收。当前版本以确定性证据、不可变评估结果和
使用者人工决定完成闭环；自动评估策略、自动分支和未来计划模板只在以后具备明确私人
规则与模板时作为可选增强，不属于当前核心缺陷。

#### P4a-1：周完成与症状趋势

状态：已由项目经理验收。

- 服务端按 Tracker 计划时区固定生成包含当前周的最近八个周一至周日自然周，客户端
  不能扩大时间范围；响应明确给出范围、时区和生成时间。
- 任务统计直接读取各日期任务实例，并按当天生效的计划版本过滤；只以任务实例自身的
  `planned/completed/skipped` 状态计数，训记或 Garmin 关联不会被当成完成。
- 每个日期从已保存的追加式身体反馈中取左右膝最高疼痛和最严重安全级别；周汇总同时
  展示反馈天数与应反馈天数，缺失反馈不按零分处理，也不重新推断安全级别。
- 趋势 API 只返回周界限、任务计数、完成率、疼痛、反馈覆盖和绿黄红天数；不返回备注、
  私人阈值、训练处方、外部来源标识或原始数据。
- 趋势页使用轻量 HTML/CSS 图形和同步文字摘要，覆盖首次加载、错误、空数据、部分数据
  与后台更新失败；缓存内容、根 Tab 实例、滚动和其他页面草稿保持不变。
- 本切片不计算训练负荷，不调用 AI，也不包含医学诊断、因果结论、睡眠、阶段评估或通知。

#### P4a-2：训练负荷与数据覆盖趋势

状态：已由项目经理验收。

- 仍按 Tracker 计划时区和当天有效计划版本统计最近八个自然周；负荷只来自用户明确
  完成的任务，待完成、跳过以及未确认的外部记录均不参与。
- 每个任务实例只计一次：时长依次采用手工实际记录、已确认 Garmin 活动、已确认训记
  训练；距离依次采用手工实际记录和已确认 Garmin 活动。来源版本过期、待复核、跨日期
  和与计划无关的记录均排除。
- 同一来源的多条不同记录按稳定记录标识去重后汇总；Garmin 与训记同时关联到同一任务
  时不相加，避免把同一次训练重复计算。
- 周汇总展示完成训练天数、已测量时长与覆盖任务数、已测量距离与覆盖任务数，以及手工、
  Garmin、训记和未测量的任务覆盖。缺少数据保持“未测量”，不解释成零或下降。
- API 只返回周级聚合数字和来源覆盖计数，不返回任务备注、动作明细、外部记录标识、原始
  数据、处方或私人安全阈值；趋势页明确说明训练和身体反馈只是同期展示，不表达因果关系。
- 查询复用 P4a-1 的同一聚合 API，并与任务、反馈和计划版本读取并行；没有新增数据库
  Schema 或 migration，根 Tab、缓存、后台错误保留和草稿不变量保持不变。

#### P4b-1：AI 分析任务与只读计划调整建议

状态：已由项目经理验收；本切片不提供接受、拒绝、应用或回滚。

- 只在使用者明确点击后创建持久分析任务；任务先落库，再在同一请求内进行一次有
  超时和输出大小上限的 Provider 调用。离开页面不保证后台完成，失败和未完成状态
  可以在重新进入后继续查看或重试。
- `PlanAdvisor` 保持 Provider-neutral，DeepSeek Adapter 只在服务端读取环境配置，
  使用 JSON Output，并对空内容、截断、非法 JSON、未知字段、越界操作和安全冲突
  分别失败关闭。模型名由部署配置提供，不写入领域代码。
- 第三方上下文只包含当前有效计划的最小投影、最近十四天的结构化身体反馈和用户
  确认训练；移除身份、仓库来源、反馈备注、训练摘要、外部原始数据和凭证。数据库
  只保存上下文版本、hash、日期范围和模型/响应 hash 等审计信息。
- 每个建议同时绑定基础计划版本和创建时的计划时间线头；任何后来新增的计划版本都会
  使旧建议过期；读取时也会重建当前上下文，新增反馈、红灯、用户确认训练或日期窗口
  变化都会使旧建议失效。确定性红灯优先于模型输出，红灯只显示停止并重新评估，
  不保留训练加码操作。
- 页面提供稳定的只读入口，显示运行、失败、过期、红灯和逐项差异；没有任何计划
  写入按钮。未配置 Provider 时安全显示暂不可用，不发出外部请求。

#### P4b-2a：人工接受/拒绝与不可变计划版本应用

状态：已由项目经理验收；本切片不提供计划回滚。

- 使用者只能对当前且未过期的建议明确接受或拒绝；接受前由服务端重新核对计划、
  近期记录和安全状态，页面缓存不能替代最终校验。
- 接受时由应用根据数据库中的基础计划与已校验建议生成下一份完整不可变计划版本，
  固定从下一个计划日生效，不修改当天、历史记录或旧计划版本。
- 拒绝只记录决定，不创建计划版本；接受和拒绝都使用稳定命令、原子事件与私有镜像
  outbox，并由数据库唯一约束裁决并发决定。
- 页面先展示差异和生效日，再由使用者二次确认；本切片完成后仍不提供回滚。
- 数据库以 Tracker 上下文 revision 和事务内锁消除“校验后又新增反馈、确认训练或计划
  版本”的竞态；决定、Proposal、新计划与未来任务、事件和 outbox 全成或全败。
- 同一命令重放返回已保存决定；不同命令并发决定同一建议时只允许一条决定与至多一份
  新计划提交。后段 outbox 冲突会回滚全部决定写入，不会伪装为建议过期。

#### P4b-2b：撤销已接受的 AI 计划更新

状态：已由项目经理验收；只通过新增不可变计划版本撤销已接受的 AI 计划更新，不删除
或改写历史版本、决定和事件。

- 只有已接受且 applied plan 仍为当前时间线头的决定可以撤销；后来已有任何计划版本时
  直接阻止，不覆盖后续时间线。
- 应用从数据库中的基础计划完整快照创建下一计划日生效的 vN+1，只投影生效日起任务；
  历史任务、旧计划、原 Proposal 与接受决定保持不可变。
- 稳定回滚命令、源 applied plan 唯一约束和数据库 guard 负责幂等与并发裁决；回滚、
  新计划、未来任务、事件和 outbox 在同一 Neon transaction 中全成或全败。
- 页面展示目标版本、生效日和受影响日期，并在二次确认后由使用者创建新版本；本切片
  不提供任意历史版本选择或非 AI 计划回滚。

#### P4b-2c：AI 审计镜像收口

状态：代码、正式 `0015` 回填 migration、匿名 Schema、临时 Neon 集成和部署门禁已
由项目经理验收；未调用真实 DeepSeek，也未制造真实分析或计划决定。

- 分析任务与结构化 Proposal 分别使用稳定、provider-neutral 的私仓路径；同一 ID 的
  状态变化更新同一审计文档，不制造重复文件。
- 审计白名单只保留状态、安全错误代码、时间、上下文版本/hash/范围、计划指针、模型/
  响应 hash、已校验操作以及决定和回滚关系；不保存 prompt、Provider 原始响应、完整
  上下文、反馈/训练自由文本、外部 raw、身份或凭证。
- pending/running/failed/succeeded、proposed/expired/accepted/rejected 和 rollback 的
  PostgreSQL 状态变化与对应 outbox 原子提交；稳定命令、并发决定和回滚最终只留下当前
  审计状态。GitHub 消费失败不回滚核心事实。
- `0015` 为部署前已有 job/proposal 回填当前审计快照；临时 Neon 已验证 journal、严格
  Schema、状态演进、重复幂等和事务后段冲突回滚。GitHub Adapter 继续对相同内容跳过
  无意义提交。

#### P4c-1：阶段评估会话与有效训练周证据底座

状态：已于 2026-07-26 由项目经理独立复核并验收；本切片不填写评估结果、不选择后续
分支，也不创建计划版本。

- `evaluation_sessions` 同时支持 `stage/final` 中性会话类型；当前结构化计划尚无阶段
  目标字段，因此页面只开放可审计派生的结项评估。目标日由服务端取计划时间线头完整
  计划中的最后任务日期，客户端不能提交或扩大日期。
- 到达目标日只允许使用稳定命令开启评估，不自动宣布康复完成。会话原子保存冻结证据、
  `evaluation_session_created` 事件和 GitHub outbox；同命令重放返回既有会话，后段
  outbox 冲突会回滚全部写入。
- 快照冻结触发日、基础计划、创建时计划时间线头、计划时区、证据范围和计算版本；任何
  后续计划时间线变化都会让旧会话过期，必须按新时间线重新开启。
- 周证据按历史有效计划版本汇总计划任务、用户确认完成/跳过、反馈覆盖、最高疼痛、最
  严重已保存安全级别、暂停/出差/器械受限/降级天数及可测训练覆盖。缺失反馈不当成零，
  外部关联不自动完成任务。
- 公共代码没有硬编码有效训练周阈值。未提供私人版本化 `EvaluationPolicy` 时，会话只
  标记 `needs_policy` 并展示原始证据；左右侧评估、维护/进阶/延长/人工复评和未来计划
  变化留给 P4c-2。
- 页面使用稳定 `/trends/evaluation` 入口，覆盖目标日前、可开启、已开启、过期、加载、
  错误和空计划状态；默认文案明确“评估尚未等于完成”。

#### P4c-2a：评估表单与不可变评估结果

状态：已于 2026-07-26 由项目经理独立复核并验收；本切片只保存评估事实，不判断有效
训练周、不选择后续分支，也不创建计划版本。

- 在仍有效且已开启的评估会话中，分别记录左右侧反应、力量与动作控制、负荷耐受，
  并记录目标完成情况和下一阶段意向。可选补充文字只进入私人数据域。
- 每个会话至多产生一份 `evaluation-result-v1` 不可变结果。稳定命令重放返回同一结果，
  不同命令并发由数据库唯一约束裁决，不能覆盖已经保存的事实。
- 提交事务再次锁定 Tracker 和会话，核对上下文 revision、基础计划、计划时间线头以及
  当天红灯反馈；任何变化都会失败关闭。结果、`evaluation_result_recorded` 事件和
  GitHub outbox 使用同一 Neon HTTP batch 原子提交。
- 页面保存失败会保留完整草稿和同一命令 ID；后台刷新不重建 App Shell，也不清除草稿。
  已保存页面明确说明“记录评估结果不等于完成，也不会自动修改计划”。
- 公共仓库只保存中性 Schema、执行器和匿名夹具；结果中的可选补充文字只进入私人
  PostgreSQL 和私有镜像事件，不进入公共文档、测试或日志。

#### P4c-2b：逐周人工确认与下一步决定

状态：已于 2026-07-26 由项目经理独立复核并验收；S12 当前人工确认范围完成。本切片
不自动判定有效训练周，不生成建议，也不创建或修改计划版本。

- 每个已保存评估结果至多产生一份 `evaluation-decision-v1` 不可变决定。使用者必须逐周
  引用会话快照冻结的 `weekStart/weekEnd`，选择计为有效、不计为有效或暂不确定；客户端
  不能增加、删除或改写周证据。
- 最终分支固定为维持、专项进阶、延长当前阶段或专业复评，并明确标记为使用者决定。
  当前或冻结证据有红灯时只允许专业复评；有黄灯或缺少完整绿灯证据时不允许专项进阶。
  公共代码没有新增完成率、反馈天数、连续周数或其他个性化阈值。
- 页面先完整预览逐周选择、原因、最终分支和可选说明，再二次确认不可变保存。失败保留
  草稿与稳定命令 ID，慢网双击只提交一次；后台安全状态变化会退出确认并保留草稿。
- 提交事务锁定 Tracker、会话和结果，核对上下文 revision、会话 open、基础计划、时间
  线头、canonical 结果和当前红灯。决定、`evaluation_decision_recorded` 事件与 GitHub
  outbox 在同一 Neon HTTP batch 中原子提交；唯一约束裁决并发，后段冲突整体回滚。
- 保存后只显示“已由你完成选择”。专业复评分支提示联系专业人员；其他分支提示未来
  计划仍需独立制定、查看差异并确认，不宣称康复完成或计划已经更新。

#### P4 可选增强

以下能力只在以后具备明确私人规则或完整私人模板时扩展，不属于当前核心闭环的缺陷：

- 若以后需要自动辅助判断，必须先提供明确的私人版本化 `EvaluationPolicy`；当前人工
  决定保持可审计事实，不反向改写。
- 维护、专项进阶或延长后的未来计划建议、差异预览与人工确认；缺少完整模板时不得创建
  计划版本。
- 睡眠背景、阶段评估后续体验和通知。

核心首版退出条件已满足：S08 与 S12 当前人工确认范围均已通过；非法、红灯、过期和
并发建议均不能改写计划，接受、拒绝与回滚在计划时间线上可审计。

### P5：可选体验增强

P0-P4 核心首版继续保持完成。P5 是使用者主动追加的近期增强，不倒退已经验收的核心
里程碑。

#### P5a-1：DeepSeek 私人凭证接入与现有建议链路启用

状态：已于 2026-07-26 由项目经理验收。生产保持未连接，不在开发或部署过程中调用真实
DeepSeek；后续由使用者本人在设置页录入并完成一次受控验证。

- 设置页只接收 DeepSeek API Key，验证成功后清空输入；已保存值只以 AES-256-GCM 密文
  留在通用 `integration_credentials`，浏览器不能回读。
- 新 Key 先经过 server-only、匿名、小输出和限时验证；验证成功才原子替换旧密文，认证、
  限流、超时、不可用或非法响应均保留原有可用凭证并返回安全状态。
- P4b 的既有 `PlanAdvisor`、分析任务、Proposal、接受/拒绝、回滚和审计镜像保持不变；
  生成建议时按 Tracker 解密取得私人 Key，模型、端点、超时和输出上限仍由服务端控制。
- 分析认证失效后，连接状态进入“需要更新”；其他临时故障显示“暂时不可用”。状态通过
  共享 Query cache 精确更新，不重建 App Shell 或清除其他页面草稿。

#### P5a-2：Garmin 睡眠与步数

状态：P5a-2a“单日睡眠与步数底座”、P5a-2b“有限追赶与前台恢复”和
P5a-2c“恢复趋势与 DeepSeek 背景证据”均已于 2026-07-27 由项目经理验收。
继续沿用 Garmin 只读、来源可审计和不自动完成任务的边界。

- 固定版 Garmin Runtime 对用户明确选择的一天各读取一次日汇总与睡眠；任一请求发生
  技术失败时整日失败，不把失败伪装成无数据。
- `daily_wellness` 使用通用 `external_records` 和独立日期同步状态，按日期稳定标识和
  内容 hash 幂等更新；它不进入任务关联候选，也不改变任务状态或身体反馈安全结果。
- 今日与日历日详情只返回睡眠、步数、数据可用状态和最近同步时间的白名单摘要；缺失
  数据与真实零步保持区分，月历汇总仍不统计该数据。
- P5a-2b 复用通用追赶执行器，但以 `garmin_wellness` 独立保存日期状态、游标和自动
  恢复租约；手工和前台单批最多五天，首次从 Tracker 开始日追赶，完成后重叠两天。
  首个失败日停止并保留游标，前台只在首次在线或恢复联网时尝试一个到期批次。
- activity 与 wellness 继续使用彼此独立的业务游标和到期状态；`0019` 只为通用加密凭证
  增加共享 Provider I/O 租约。随机 owner、到期恢复和 owner 条件写回防止并发刷新覆盖；
  用户替换 Token 会使旧请求立即失去写回资格，租约忙不会污染任一业务日期状态。
- 前台只保留一个 Garmin 恢复协调入口，每次触发严格依次尝试 activity 和 wellness；
  非认证类跳过或临时失败不阻断后一类。首次遇到共享 I/O 租约忙时只安排一次有界重试，
  避免两个独立组件反复竞争而让 wellness 长期没有推进机会。
- P5a-2a 的外部记录 JSON Schema 公私同步新增 `daily_wellness` kind；P5a-2b 的 `0019`
  不改变镜像业务 Schema。
- P5a-2c 在既有 8 周趋势中增加睡眠、评分和步数的中性平均及覆盖天数；当天尚未结束的
  步数不进入周平均，缺失不会补零，页面只说明同期参考而不推断因果或好坏。
- 现有 AI 最近 14 天上下文只加入日期、睡眠总时长/评分、步数及缺失/当天未完结标记；
  不发送睡眠分期、起止时间、目标步数或 Provider 原始数据。恢复数据变化参与上下文
  hash 和 revision，使旧建议按既有机制过期，但不改变红黄绿、安全策略或任务状态。

#### P5a-3：DeepSeek 模型选择与入口可发现性

状态：已由项目经理验收。代码、迁移、部署和只读生产验收已完成；真实 Flash/Pro
连接测试仍需使用者本人在设置页明确点击，不能把当前结论表述为 Provider 调用成功。

- 设置页只提供 Flash（日常建议）和 Pro（更深入）两个选项；未保存偏好的既有 Tracker
  默认 Flash，无需重新录入 API Key。选择独立保存在 provider-neutral
  `integration_preferences`，不进入凭证密文或同步状态。
- 保存模型不调用 DeepSeek、不消耗额度，也不改写既有分析、建议、决定、回滚或审计；
  下一次新分析读取服务端保存的选择，并在任务审计中记录实际模型。
- 已连接状态提供“测试当前模型”和“生成训练调整建议”。前者只发送一次固定匿名 JSON
  小请求，不含计划、训练、反馈、恢复或身份数据，也不创建分析任务；后者进入
  当前入口 `/plan/advice`（旧 `/trends/advice` 兼容跳转），继续由使用者明确点击生成建议。
- 正式建议和匿名测试均固定关闭 thinking，并对模型、JSON、响应大小和安全错误分类
  严格校验。部署环境不再使用 `DEEPSEEK_MODEL` 覆盖 Tracker 选择。

#### P5a 本轮完成范围

- DeepSeek 设置页支持私人 API Key 验证、加密保存和状态管理，并启用既有建议、人工
  接受/拒绝和新版本回滚链路。
- P5a-3 追加模型选择、匿名连接测试和建议入口，已由项目经理验收；使用者尚未点击
  Flash/Pro 真实测试，代码部署和只读页面验收不等于 Provider 调用成功。
- Garmin 睡眠与步数已覆盖单日同步、有限追赶、前台协调恢复、今日与日历展示、最近
  8 周趋势以及 DeepSeek 最近 14 天最小背景证据。

#### P5b-1：历史数据补录与 AI 上下文透明化

状态：代码、部署和只读界面已由项目经理验收。本切片不改变 Tracker 正式开始日、常规
增量同步范围、任务完成状态或现有计划时间线。真实 Garmin、训记与恢复数据历史补录、
实际上下文预览内容及 DeepSeek 调用仍等待使用者受控端到端验收，不据只读证据宣称完成。

- 设置页提供“同步过去 7 / 14 / 30 天”，默认 14 天。服务端按计划时区换算固定范围，
  Garmin activity、Garmin daily wellness 与训记分别使用独立历史 scope 保存逐日成功、
  空记录、失败和续跑位置；历史补录不会推进或重置常规追赶 cursor。
- 历史补录每个服务端请求只处理有界日期批次；一次使用者操作会按来源顺序有限续批，直到
  所选范围完成、该来源出现安全失败或达到固定请求上限。首个失败日停止该来源并保留此前
  成功，一个来源失败不会阻止其他来源继续。页面关闭后仍从数据库游标恢复；外部记录继续
  复用稳定记录键、内容 hash 和来源版本幂等写入。
- AI Context v2 从 PostgreSQL 中读取最近 14 天已持久化的完整当前计划、结构化反馈、
  用户确认任务、Garmin 活动与恢复参考及训记力量明细。外部记录只作为 observed evidence，
  不能伪装成计划任务完成；同一次训练的跨来源记录以关联或时间重叠分组，提示模型不得
  重复计量。
- 已保存反馈备注只以使用者可见的限长 observation 进入本次上下文；发送前页面展示日期
  范围、计划、反馈、各来源记录与覆盖/空记录/失败情况。使用者确认后，服务端再次核对
  `previewHash`，一致时才创建分析任务并调用一次已选择的 Flash/Pro。
- 建议页首步只整理并展示本次分析内容，不创建分析任务；使用者查看上下文后再明确确认
  生成建议。四种手机宽度锁定初始、预览、提交中和结果状态的紧凑按钮与无横向溢出。
- 前台自动恢复统一按 Garmin activity、Garmin wellness、训记顺序各尝试一个到期批次；
  Garmin 每日 Cron 协调 activity 与 wellness，独立训记 Cron 补齐日常兜底。历史 7/14/30
  天补录不会被任何自动恢复或 Cron 触发。
- P5b-1 真实体验返修已由项目经理验收：补录页从服务端恢复最近范围和三个来源的
  逐日汇总，区分本次补录有记录的日期与当前范围内已保存的记录日期，避免把日常同步事实
  混入本次补录计数；日期链接会驱动持久日历实例同步 URL、月份、选中日、详情和焦点，
  browser back/forward 继续一致。计划开始日前仍保留未开始语义，同时不隐藏已经持久化的
  来源事实。完成批次后只失效涉及的日期和月份缓存，不触发全局刷新。一次点击自动续批的
  真实 Provider 行为仍等待使用者本人受控执行；旧页面中尚未完成的逐日状态不代表返修失败。

#### P5b-2：外部数据每日后端同步，移除网页自动同步

状态：实现、测试和部署待项目经理复核；本切片不调用真实 Garmin、训记或其他 Provider，
不创建生产健康/训练数据。

- 受保护网页不再挂载 Garmin/Xunji foreground recovery coordinator。冷启动、首次在线、
  offline→online、focus、visibility、Tab/路由切换和 Query refetch 均只读取已持久化的
  canonical integration status 与外部记录；GitHub mirror 的既有前台/after-response/Cron
  语义保持不变。
- Garmin 每日 Cron 继续在一次调用内按既有顺序覆盖 activity 与 daily wellness；训记使用
  独立 Cron。两者均配置为 `0 20 * * *` UTC，即北京时间凌晨 4:00–4:59 的 Hobby 小时窗口，
  不宣称精确 4:00。既有 CRON_SECRET 鉴权、有界批次、lease、cursor、幂等和 cooldown 保持。
- Vercel Hobby 不会替失败 Cron 自动重试；失败日由数据库日期状态和 cursor 保留，下一天
  由同一 Cron 续跑，期间使用者可手动同步兜底。Cron 仍不触发 7/14/30 天历史补录。
- 保留设置页手动 Garmin/Xunji 同步、状态展示、30 秒冷却、operation lease、0-record、
  错误文案、历史补录、AI 上下文、任务完成/关联和 `startedOn` 语义；无数据库 migration。
- RED/GREEN 门禁覆盖 AppProviders 不挂载 Garmin recovery、各前台生命周期零 Provider
  recovery POST、GitHub recovery 仍存在，以及两条 Cron schedule、Garmin activity+wellness
  协调器、训记有界批次和安全鉴权响应。

#### P5c：康复计划入口与康复助手重构

状态：独立验收返修完成、等待项目经理复核。

- 顶层导航调整为“今日 / 日历 / 计划 / 设置”；计划作为延迟加载的持久根 Tab，集中展示
  当前周次、目标、下一训练、康复助手、待确认建议和回顾/版本/评估/档案/记忆入口。
  旧 `/trends*` 保留兼容跳转，根 Tab 的最新意图继续同时支配 URL、可见内容、
  `aria-current` 和 back/forward。
- `/plan/conversation` 提供连续只读对话。Context v3 包含完整计划、当前结构化档案版本、
  有效稳定记忆、最近 20 轮及最近 14 天反馈、确认任务、Garmin/训记和恢复参考；一次补充
  历史查询必须明确且不超过 30 天。原始医疗材料、本机路径、Provider raw/ID 和凭证不
  进入模型。
- 康复档案以不可变版本保存；助手记忆只限稳定目标、偏好、时间、器械和训练习惯，可查看、
  修改和删除，不能改写安全规则、档案或计划。普通对话不产生正式业务写入。
- 今日保留结构化快速反馈并增加“告诉康复助手”；日历可按日期、任务、Garmin 或训记记录
  预关联。自然语言只生成可编辑确认卡，明确确认后才保存正式反馈并执行服务端确定性
  红黄绿；未来日期禁止，离线只保留未发送草稿。
- 对话中的“生成调整方案”复用既有预览、明确确认、DeepSeek Flash/Pro、差异、接受/
  拒绝/稍后、不可变新版本和回滚链路。Context v3 的档案、记忆和来源对话参与 hash，
  任一变化都会让旧建议过期；GitHub 仍只镜像最小状态、模型、版本、计数和 hash，不镜像
  对话正文、档案或记忆。
- `0021` migration 新增对话、轮次、记忆、档案和建议来源关联表/列；公共与私人审计
  Schema 仅扩展 Context v3 枚举。发布过程不调用真实 DeepSeek、Garmin 或训记，也不为
  验收创建真实反馈、对话、建议或计划版本。
- 独立验收返修后，计划默认页延迟加载并复用同一康复助手会话，直接提供最近回复、输入框
  和完整对话入口；摘要不再常驻版本号，版本只保留在“计划版本”二级入口。
- 手动修改或删除助手记忆与 Tracker Context revision 使用同一 Neon 原子 batch；任一
  后段失败时记忆与 revision 均回滚。
- 同一成功助手轮次的反馈确认由数据库行锁串行化，event、GitHub outbox 与 turn pointer
  在同一语句中提交；不同命令并发只形成一条 canonical 反馈，同命令稳定重放，后段冲突
  整体回滚。未来日期拒绝、可编辑草稿和确定性红黄绿保持不变。
- 计划摘要原“当前周次”只按 `startedOn` 计算日历差，暂停和接续排期不会修改该起点，
  因而不能代表有效训练阶段；DTO 与界面现明确称为“起算后的日历周”，并提示不等于有效
  训练阶段，没有新增医学阶段规则。

#### P5d：训记 Provider 30 秒冷却期与界面状态

状态：项目经理验收通过。

- `integration_sync_state.cooldown_until/cooldown_kind` 通过正式 `0022` migration 保存
  provider-level canonical cooldown；原子 claim 发生在缓存 miss、Provider full-data 读取前，
  operation lease 释放后仍由服务端拦截 automatic/manual/history/Cron 的重复调用。正常
  cooldown 不写失败日、cursor 或连接状态；Provider `retry_after_ms` 会以服务端时间扩展
  到 canonical deadline。
- 设置一级列表和 `/settings/xunji` 详情复用服务端 `cooldown`、绝对
  `retryAvailableAt` 与 `serverNow`；正常冷却、in-flight 和真实 `rate_limited` 使用不同
  文案，倒计时到期自动清理。Garmin 日常语义与租约保持不变。
- Vitest、Python、format、lint、typecheck、production build 和相关 320/375/390/430
  浏览器门禁已通过；项目经理独立复跑真实临时 Neon cooldown 集成 2/2，Production
  `0022` migration 与 journal 对账通过，部署后 HEAD、origin/main、health/database 与匿名
  Xunji `401` 均已核对。
- 本切片未主动调用真实 Provider，下一次自然同步由使用者体验验证；不将部署门禁或匿名
  route 验证表述为真实训记业务调用。

#### 后续未开始增强

- 用户主动授权的通知、Web Push 和 App Badge。
- 第二个 Tracker 模块、FIT 导入、更多自动 `EvaluationPolicy` 与未来计划模板。

以上后续增强未进入本轮范围，继续延后，不属于当前核心闭环或 P5a 指定范围的缺陷。

## 近期实施顺序

1. P0b-r1a、P0b-r1b 与 P0c-r1 均已由项目经理验收；更多首次加载性能优化降为体验
   观察项。
2. P1a-1、P1a-2 与 P1a-3 均已验收；真实数据试用留待以后录入轮换后的 Key。
3. P0c-1、P0c-2、P0c-3 均已验收，P0 核心实现完成；真机长期体验持续验收。
4. P1b-1、P1b-2、P1b-3 均已验收，P1 当前实现阶段收口。
5. P2a 至 P2b-4 均已验收，P2 代码实现收口；真机发布验收继续保留。
6. P3a-2、P3a-3a 与 P3a-3b 已验收；P3a-3c 已完成首次平台 Cron 空队列生产验收。
   P3a-1 的首条真实私仓写入仍等待自然产生的 outbox 后受控验收。
7. P3b-1、P3b-2a、P3b-2b、P3b-2c、P3b-2d、P3b-2e 与设置页移动端显示收口均已
   验收；首次平台 Cron 调用继续观察。后续 P5a-2 的睡眠和步数指定增强也已完成；FIT
   导入继续延后。
8. P4a-1、P4a-2、P4b-1、P4b-2a、P4b-2b、P4b-2c、P4c-1、P4c-2a 与 P4c-2b 均已
   验收，P4 核心首版完成；自动 `EvaluationPolicy`、自动分支与未来计划模板只在以后有
   明确私人规则时增强，未确认的个性化阈值仍不得由公共代码、AI 或开发者经验发明。
9. P5 是使用者主动追加的增强；P5a-1 DeepSeek 私人凭证接入、P5a-2a/P5a-2b/P5a-2c
   Garmin 睡眠与步数链路，以及 P5a-3 模型选择、匿名连接测试和建议入口均已验收，
   本轮指定范围 100% 完成。真实 Flash/Pro 测试仍待使用者本人点击；通知、第二
   Tracker、FIT 和更多自动评估规则未开始并继续延后。
10. P5b-1 历史数据补录、AI Context v2、发送前上下文预览和三来源日常恢复的代码、部署
    与只读界面已由项目经理验收；真实三 Provider 历史补录、上下文内容及 DeepSeek 调用
    仍等待使用者受控端到端验收，不会由开发或只读验收主动触发。一键有限续批、补录结果
    口径、持久日历 URL 联动和计划前来源事实展示均已通过项目经理只读生产复核。
11. P5c 康复计划入口与康复助手重构已完成独立验收返修，等待项目经理复核；真实 Provider 调用和任何
    私人记录创建继续留给使用者明确触发，不属于开发与只读生产验收。

每个切片同时包含界面、领域规则、数据迁移、自动化测试和运维状态，不能把测试、
离线和失败处理统一推迟到功能列表末尾。

## 持续观察项

- 一次全新直达 `/calendar?date=2026-08-04` 出现 React #418 水合错误；同轮全新直达根页
  没有错误，且 P1a-r1 未修改初始日期或根壳逻辑。该现象不阻塞本次关联返修验收，继续
  作为独立的水合稳定性观察项保留，不在本切片扩修。
- iPhone 主屏应用的飞行模式冷启动、恢复联网和真实 Service Worker 更新仍需真机发布
  验收；自动化门禁不能替代该项。
- GitHub 私仓镜像的首条自然业务 outbox 仍需受控验证真实写入、相同内容不产生无意义
  commit 以及生产状态计数；不得为验收制造健康记录。
- Garmin 每日恢复 Cron 的首次平台实际调用继续观察；代码、部署和 Cron 注册已经验收，
  但不能据此虚报首次平台调用已发生。
- Neon URI 暴露风险已由使用者知情接受并暂缓轮换；风险仍存在，不得表述为安全处置
  完成。

## 角色与交付职责

项目只有一个使用者，但按成熟项目的职责分工组织工作；同一执行者可以承担多个角色，
每项交付仍需从对应视角审查。

| 角色            | 主要职责                                         | 权威文档                                                                |
| --------------- | ------------------------------------------------ | ----------------------------------------------------------------------- |
| 产品经理        | 目标、范围、优先级、用户旅程和验收标准           | `product-requirements.md`、本文                                         |
| 系统架构师      | 上下文、模块边界、数据流、技术决策和质量属性     | `architecture/overview.md`、`architecture/technology-selection.md`、ADR |
| 产品设计师      | 信息架构、交互、视觉系统和可访问性               | `design/product-and-visual-system.md`                                   |
| 前端工程师      | PWA、App Shell、客户端状态、表单、缓存和离线体验 | `architecture/client-data-and-navigation.md`                            |
| 后端/数据工程师 | API、领域命令、PostgreSQL、作业、同步和 Schema   | `architecture/data-and-sync.md`、`schemas/`                             |
| 集成工程师      | DeepSeek、Garmin、训记、GitHub 镜像及故障隔离    | `architecture/data-and-sync.md`、`operations/garmin.md`                 |
| QA              | 系统不变量、自动化分层、核心旅程和发布判定       | `testing/core-scenarios.md`                                             |
| 安全负责人      | 身份、权限、Secrets、隐私和威胁边界              | `security.md`                                                           |
| 运维负责人      | 部署、迁移、监控、恢复、回滚和生产冒烟           | `operations/deployment.md`                                              |

## 依赖与风险

| 风险或依赖                      | 处理方式                                                                             |
| ------------------------------- | ------------------------------------------------------------------------------------ |
| 当前动态路由造成日期和 Tab 延迟 | P0 先完成客户端 App Shell、查询缓存和局部加载                                        |
| iOS Background Sync 不可靠      | 依靠 IndexedDB，并在多个前台时机重放和显示真实同步状态                               |
| Vercel 与 Neon 可能跨区         | 使用生产追踪确认区域，迁移前比较数据库时延和函数配置                                 |
| Garmin 非官方登录可能变化       | Adapter 隔离、固定版本、Token 失效提示和 FIT/CSV 导入                                |
| 训记 Key 权限宽于第一版用途     | 后端加密保存、只读 Adapter、接口白名单和不记录 Authorization                         |
| DeepSeek 超时、限流或输出异常   | 持久化分析作业、严格 Schema 校验、失败可重试且不影响原始记录                         |
| GitHub Contents API 冲突或限流  | outbox 串行目标路径、SHA 重读、退避重试和全量镜像重建                                |
| Vercel Hobby Cron 每日且不重试  | 响应后尽力消费、应用/手动触发、持久 outbox 和每日修复                                |
| 私人数据泄漏                    | 公私仓分离、服务端 Secrets、最小权限、匿名测试和本机缓存清理                         |
| 数据库凭证轮换                  | 一次任务记录暴露风险已由使用者知情接受并暂缓轮换；风险仍存在，不能表述为安全处置完成 |

风险的架构处理写入对应专题文档；本文只维护其对优先级、交付顺序和发布门槛的影响。

## 发布门禁

每个里程碑进入可用状态前必须满足：

1. 静态检查、类型检查、领域规则和对应自动化测试通过。
2. 数据库变更使用正式 migration，并验证事务、幂等和 outbox。
3. 核心用户旅程完成浏览器测试；PWA、离线、缓存或输入变更完成 iPhone 真机检查。
4. 生产冒烟验证身份门禁、健康检查、静态资源和受影响功能。
5. 不存在会造成数据丢失、重复、越权、错误计划或红灯规则失效的已知问题。

详细用例和分层见[核心场景测试与发布保障](testing/core-scenarios.md)。
